<h1>Messages</h1>

<% if user_signed_in? %>
    <%= render 'messages/form' %>
<% end %>

<div id="messages" class="<%= Message.count.odd? ? 'odd' : '' %>">
    <%= render partial: :product, collection: @messages %>
</div>